<!--<% @recipes.each do |recipe| %>
  <%= recipe %>
<% end %>-->


<table class="table table-hover">

<% @recipes[0].each do |key,value| %>
  <% if (key == "recipe") || (key == "description") %>
    <th><%= key.capitalize %></th>
  <% end %>
<% end %>


<% @recipes.each do |recipe| %>
  <tr>

    <% if recipe["description"] == nil %>
        <% next %>
    <% else %>

          <% recipe.each do |key,value| %>
            <% if (key == "description") && (value != nil) %>
              <td><%= value[0, 100] %>...</td>
            <% elsif key == "recipe" %>
              <td><%= value %></td>
            <% end %>
          <% end %>

      <% end %>

  </tr>
<% end %>

</table>


  </body>
</html>
